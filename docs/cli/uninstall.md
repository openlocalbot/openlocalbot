---
summary: "CLI reference for `openlocalbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openlocalbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openlocalbot uninstall
openlocalbot uninstall --all --yes
openlocalbot uninstall --dry-run
```
